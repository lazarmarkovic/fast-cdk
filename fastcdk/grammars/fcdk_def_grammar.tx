MetaTemplate:
    name=ID "{"
        template_file=TemplateFileAssign
        default_path=DefaultPathAssign
        deps=DepsSection?
        env_vars=EnvVarsSection?
        default_inputs=DefaultInputsSection
    "}"
;

TemplateFileAssign:
    "template_file" "=" val=PATH
;  
DefaultPathAssign:
    "default_path" "=" val=PATH
;

PATH: /[A-Za-z0-9_\-\.\/\\:]+/ ;

// -----------------------------------------------------------------------------
// DepsSection: a list of DepEntry’s inside “deps { … }”
// -----------------------------------------------------------------------------
DepsSection:
    "deps" "{"
      entries*=DepEntry
    "}"
;

// -----------------------------------------------------------------------------
// DepEntry: 
//   key=ID "=" type=ID [ "{" props+=InputAssign* "}" ]   // named, opt-block
// | type=ID       [ "{" props+=InputAssign* "}" ]        // unnamed, opt-block
// -----------------------------------------------------------------------------
DepEntry:
      key=ID "=" type=ID ( "{" props+=InputAssign "}" )? "from" source=ID
    |               type=ID ( "{" props+=InputAssign "}" )? "from" source=ID
;


// -------------------------------------------------------------------
// EnvVarsSection: zero or more EnvVar entries inside "env_vars { … }"
// -------------------------------------------------------------------
EnvVarsSection:
    "env_vars" "{"
        entries*=EnvVar
    "}"
;

// -------------------------------------------------------------------
// EnvVar: 
//   key=ID "=" ref=RefPath "=" val=PATH
// 
//  - key: your env-var name
//  - RefPath: dot-separated identifier (e.g. BuildParams.Root.DomainName)
//  - val: bare token (letters, digits, dashes or dots)—no quotes needed
// -------------------------------------------------------------------
EnvVar:
    key=ID "=" ref=RefPath "=" val=PATH
;

// -------------------------------------------------------------------
// RefPath: one or more ID segments separated by "."
//    e.g. BuildParams.Root.DomainName
// -------------------------------------------------------------------
RefPath:
    parts+=ID["."]
;


// default_inputs { k = v; ... }
DefaultInputsSection:
    "default_inputs" "{"
        id_prefix=IdPrefixAssign
        name_prefix=NamePrefixAssign
        class_prefix=ClassPrefixAssign
        class_name=ClassNameAssign

        inputs*=InputAssign
    "}"
;

IdPrefixAssign:
    "id_prefix" "=" val=Value
;

NamePrefixAssign:
    "name_prefix" "=" val=Value
;

ClassPrefixAssign:
    "class_prefix" "=" val=Value
;

ClassNameAssign:
    "class_name" "=" val=Value
;

InputAssign:
    key=ID "=" val=Value
;

// values in default_inputs can be int, float, bool or bare strings
Value:
      FLOAT
    | INT
    | BOOL
    | PATH
    | ID
    | NULL
;

BOOL:  "true" | "false" ;
NULL:  "null" ;

Comment: /#[^\n]*/ ;
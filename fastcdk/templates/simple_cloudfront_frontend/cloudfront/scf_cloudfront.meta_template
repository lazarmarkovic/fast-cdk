name < SCF S3 Bucket
template file < s3_scf.template

env
  BuildParams.SimpleCloudFrontFrontend.S3BucketName < scf-bucket
  BuildParams.SimpleCloudFrontFrontend.CloudFrontConnectionSecret < my-secret-string
  BuildParams.SimpleCloudFrontFrontend.CloudFrontCertificateARN < my-cf-cert

dep
  Simple Network With NAT

input
  idPrefix < Scf
  namePrefix < scf
  classPrefix < null